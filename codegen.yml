schema: http://localhost:3000/graphql
generates:
  ./src/types/graphql/index.d.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      contextType: ../../datasources#TResolverContext